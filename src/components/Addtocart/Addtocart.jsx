import React from 'react'

function Addtocart({ product }) {

    console.log(product);

    return (
        <div>
            <button>Add to cart</button>
        </div>
    )
}

export default Addtocart;